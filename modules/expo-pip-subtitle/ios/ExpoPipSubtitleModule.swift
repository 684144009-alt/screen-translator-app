import ExpoModulesCore
import AVKit

public class ExpoPipSubtitleModule: Module {
  public func definition() -> ModuleDefinition {
    // ชื่อ Module ที่จะไปเชื่อมกับ React Native
    Name("ExpoPipSubtitle")

    // ฟังก์ชันที่ 1: เปิดระบบจอเล็ก (PiP)
    Function("startPiP") { (text: String) -> String in
      // ⏳ (เดี๋ยวเราจะมาเขียนโค้ดเรียกหน้าต่าง PiP ของจริงตรงนี้)
      print("[Native] iOS ได้รับคำสั่งเปิด PiP ด้วยข้อความ: \(text)")
      return "✅ iOS: เปิดระบบจอเล็กเรียบร้อย!"
    }

    // ฟังก์ชันที่ 2: ปิดระบบ
    Function("stopPiP") { () -> String in
      print("[Native] iOS ได้รับคำสั่งปิด PiP")
      return "🛑 iOS: ปิดระบบเรียบร้อย"
    }

    // ฟังก์ชันที่ 3: อัปเดตข้อความแปลภาษาแบบ Real-time
    Function("updateText") { (text: String) -> String in
      print("[Native] iOS อัปเดตคำแปลเป็น: \(text)")
      return "🔄 iOS: อัปเดตคำแปลสำเร็จ"
    }
  }
}
